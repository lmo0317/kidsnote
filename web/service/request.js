const year=document.querySelector('#year');for(let value=new Date().getFullYear();value>=2010;value--){const option=document.createElement('option');option.value=value;option.textContent=`${value}년`;year.append(option)}
document.querySelector('#public-request').addEventListener('submit',async event=>{event.preventDefault();const button=document.querySelector('#submit'),result=document.querySelector('#result');button.disabled=true;button.textContent='접수 중…';result.hidden=true;try{const response=await fetch('/kidsnote-api/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:document.querySelector('#username').value,password:document.querySelector('#password').value,email:document.querySelector('#email').value,year:year.value,includeNotices:document.querySelector('#include-notices').checked,folderMode:'day',deliveryNote:document.querySelector('#delivery-note').value})});const body=await response.json();if(!response.ok)throw new Error(body.error||'신청을 접수하지 못했습니다.');document.querySelector('#password').value='';result.className='result';result.innerHTML=`신청이 접수되었습니다.<br><small>접수번호 ${body.id.slice(0,8)} · 관리자가 확인 후 이메일로 보내드립니다.</small>`;result.hidden=false;event.target.reset()}catch(error){result.className='result error';result.textContent=error.message;result.hidden=false}finally{button.disabled=false;button.textContent='신청 접수하기'}});
